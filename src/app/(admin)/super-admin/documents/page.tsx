import DocumentDesigner from "./DocumentDesigner";

export const metadata = {
  title: "Document Design System | ABCD Admin",
  description: "Create and manage document layouts for certificates, ID cards, and official reports.",
};

export default function DocumentsPage() {
  return <DocumentDesigner />;
}
